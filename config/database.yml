default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: postgres
  password: 780732 # ← Add this line
  host: localhost
  port: 5432

development:
  <<: *default
  database: medical_portal_development
